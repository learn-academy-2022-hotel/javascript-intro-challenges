// // Write a function named marco that returns "polo".
const marcoPolo= () => {
    return "polo"
}
console.log (marcoPolo())

// Write a function named greeting that takes a name as an argument and returns "Welcome, <person's name here>!"
const greeting= (name) =>{
return `welcome, ${name}!`
}
console.log (greeting("Raquel"))

// Write a function named oddOrEven that takes a number as an argument and returns whether the number is odd or even.
const oddOrEven= (num) => {
    if (num === num % 2){
        return "your number is odd"
     
    } else {
        return "your number is even"
    }
}
console.log (oddOrEven(7))

// Write a function named triple that takes a number as an argument and returns the result of that number multiplied by 3.
const triple= (value) => {
return `${value * 3}`
}
console.log (triple(2))


// Write a function named multiply that takes two numbers as arguments and returns the result of the numbers multiplied together.
const multiply= (numOne, numTwo) =>{
    return `${numOne*numTwo}`
}
console.log (multiply(3,4))
// Write a function named divisibleBy that takes two numbers as arguments and returns whether the first number is evenly divisible by the second so that divisibleBy(10, 5) logs "10 is evenly divisible by 5".

const divisibleBy= (valueOne, valueTwo) =>{
    return `${valueOne} is evenly divisible by ${valueTwo}`
}
console.log(divisibleBy(10,5))


// Write a function named assignGrade that takes a number score as an argument and returns the letter grade for the score.

const assignGrade= (gradeOne, gradeTwo, gradeThree, gradeFour, gradeFive) =>{
    if 
}

// pseduo code: if my grade is 90 or more it will be an A
// if my grade is less than 90 but greater than 80, it will be a B
// if my grade is less than 80 but greater than 70, it will be a C
// if my grade is less than 70 but greater than 60, it will be a D
// if my grade is less than 60 then my grade will be an F 
// will need 5 inputs for each grade, will have to use "if" statement because we have multiple conditions

// Write a function named isLonger that takes two strings as arguments and returns the string that contains the most characters.

// Write a function named greaterNum that takes two numbers as arguments and returns whichever number is the greater (higher) number.

// -. Write a function named yelling that takes a string as an argument and return the string in all uppercase case letters.

